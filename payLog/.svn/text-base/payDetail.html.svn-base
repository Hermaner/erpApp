<!DOCTYPE html>
<html>

	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1,maximum-scale=1,user-scalable=no">
		<meta name="apple-mobile-web-app-capable" content="yes">
		<meta name="apple-mobile-web-app-status-bar-style" content="black">
		<title>定金单详情</title>
		<link rel="stylesheet" href="../css/mui.css">
		<link rel="stylesheet" href="../css/styleAll.css">
	</head>

	<body class="pay-bc" id="vue">
		<header class="mui-bar mui-bar-nav">
			<a class="mui-action-back mui-icon mui-icon-left-nav mui-pull-left"></a>
			<h1 class="mui-title">{{type=="0"?"定金单详情":"新建定金单"}}</h1>
		</header>
		<div class="mem-keep-out" v-if="showCardpay">
			<div class="mem-con">
				<h3>请输入卡号和密码</h3>
				<button class="layermend" @click="closeCardpay()"></button>
				<div class="mem-text">
					<ul class="mui-table mui-table-view">
						<li class="mui-table-view-cell">
							<div class="mui-input-row">
								<label>会员卡</label>
								<input type="number" v-model="memberCardNumber" placeholder="请输入会员卡号">
							</div>
						</li>
						<li class="mui-table-view-cell">
							<div class="mui-input-row">
								<label>密码</label>
								<input type="password" v-model="password" placeholder="请输入密码">
							</div>
						</li>
					</ul>
				</div>
				<div class="mem-btn flexCon">
					<button class="flexOne" @click="closeCardpay()">取消</button>
					<button class="flexOne" @click="cardPay()">保存</button>
				</div>
			</div>
		</div>
		<div class="mui-content" v-show="showBody">
			<ul class="mui-table mui-table-view" v-if="showInput">
				<li class="mui-table-view-cell payMoney fontOrange">
					<div class="flexCon">
						<label>金额：</label>
						<input class="flexOne" type="number" v-model="items.amount" placeholder="请输入金额" onchange="E.IsNum(this)">
					</div>
				</li>
				<li class="mui-table-view-cell noPadding">
					<div class="flexCon">
						<label>付款人：</label>
						<input class="flexOne" type="text" v-model="items.payer" placeholder="请输入姓名">
					</div>
					<div class="flexCon">
						<label>联系方式：</label>
						<input class="flexOne" type="text" v-model="items.mobilephone" placeholder="请输入手机号码">
					</div>
					<div class="flexCon">
						<label>备注：</label>
						<input class="flexOne" type="text" v-model="items.memo" placeholder="请输入备注">
					</div>
				</li>
				<li class="mui-table-view-cell noPadding payText">
					<div class="flexCon">
						交易门店：{{store}}
					</div>
					<div class="flexCon">
						操作员：{{op}}
					</div>
				</li>
			</ul>
			<ul class="mui-table mui-table-view" v-if="!showInput">
				<li class="mui-table-view-cell payNum">
					<div class="flexCon">
						<div class="flexOne fontEll">定金单号：{{items.orderNumber}}</div>
						<div class="flexOne mui-text-right">创建时间：{{items.created}}</div>
					</div>
				</li>
				<li class="mui-table-view-cell payMoney fontOrange">
					<div class="flexCon">
						<label>金额：</label>
						{{items.amount}}
					</div>
				</li>
				<li class="mui-table-view-cell noPadding">
					<div class="flexCon">
						<label>付款人：</label>
						{{items.payer}}
					</div>
					<div class="flexCon">
						<label>联系方式：</label>
						{{items.mobilephone}}
					</div>
					<div class="flexCon">
						<label>备注：</label>
						{{items.memo}}
					</div>
				</li>
				<li class="mui-table-view-cell noPadding payText">
					<div class="flexCon">
						交易门店：{{items.payStore}}
					</div>
					<div class="flexCon">
						操作员：{{items.operator}}
					</div>
					<div class="flexCon">
						账户名：{{items.accountName}}
					</div>
				</li>
			</ul>
		</div>
		<div class="bottomMenu detail-menu">
			<div class="detail-btmBtn">
				<a href="#payPopover">
					<button class="mui-btn-mini" v-if="showInput">收款</button>
				</a>
				<button class="mui-btn-mini" @click="getAway()" v-if="!showInput">红冲</button>
				<button class="mui-btn-mini blackBtn" @click="printEvent()" v-if="!showInput">打印凭证</button>
			</div>
		</div>
		<div id="payPopover" class="mui-popover mui-bar-popover detail-pop">
			<div class="mui-popover-arrow"></div>
			<ul class="mui-table-view">
				<li class="mui-table-view-cell" sid="6">
					支付宝扫码
				</li>
				<li class="mui-table-view-cell" sid="15">
					微信扫码
				</li>
				<li class="mui-table-view-cell" sid="16">
					会员卡支付
				</li>
				<li class="mui-table-view-cell"  sid="14">
					刷卡支付
				</li>
				<li class="mui-table-view-cell"  sid="13">
					现金支付
				</li>
			</ul>
		</div>

	</body>
	<script src="../js/mui.min.js"></script>
	<script src="../js/vue.js"></script>
	<script src="../js/app.js"></script>
	<script src="payDetail.js"></script>

</html>